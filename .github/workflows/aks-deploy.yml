name: 🚀 AKS Deploy Pipeline (Debug + Verified Secrets)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Target environment to deploy (dev/preprod/prod)"
        required: true
        default: "dev"

permissions:
  contents: read

jobs:
  deploy-aks:
    runs-on: ubuntu-latest
    environment:
      name: ${{ github.event.inputs.environment }}
      url: https://portal.azure.com

    env:
      # 🌍 Terraform Directories
      TF_ROOT_DIR: project4-enterprise-aks-devops-platform/infrastructure/aks
      OPA_POLICY_DIR: project4-enterprise-aks-devops-platform/policies/opa

      # 🧩 Terraform Variables
      TF_VAR_environment: dev
      TF_VAR_prefix: entdevops
      TF_VAR_acr_name: devopsacr001
      TF_VAR_image_name: sampleapp
      TF_VAR_aks_cluster_name: devops-aks
      TF_VAR_aks_resource_group: rg-devsecops-dev
      TF_VAR_rg_name: rg-devsecops-dev

      # 💾 Azure Credentials for Terraform Provider
      TF_VAR_subscription_id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      TF_VAR_client_id: ${{ secrets.AZURE_CLIENT_ID }}
      TF_VAR_client_secret: ${{ secrets.AZURE_CLIENT_SECRET }}
      TF_VAR_tenant_id: ${{ secrets.AZURE_TENANT_ID }}

      # 👇 Required for Terraform Azure Provider
      ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
      ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID }}
      ARM_CLIENT_SECRET: ${{ secrets.AZURE_CLIENT_SECRET }}
      ARM_TENANT_ID: ${{ secrets.AZURE_TENANT_ID }}

    steps:
      # 1️⃣ Checkout Repository
      - name: 📦 Checkout Repository
        uses: actions/checkout@v4

      # 2️⃣ 🧾 Debug Secret Availability
      - name: 🧾 Debug Secret Availability
        run: |
          echo "🔍 Checking GitHub secrets..."
          echo "----------------------------------------"
          echo "AZURE_CLIENT_ID=${{ secrets.AZURE_CLIENT_ID }}"
          echo "AZURE_TENANT_ID=${{ secrets.AZURE_TENANT_ID }}"
          echo "AZURE_SUBSCRIPTION_ID=${{ secrets.AZURE_SUBSCRIPTION_ID }}"
          echo "AZURE_CLIENT_SECRET=${{ secrets.AZURE_CLIENT_SECRET }}"
          echo "----------------------------------------"
          echo "✅ Secrets should appear masked (***)."

      # 3️⃣ Azure Login
      - name: 🔐 Azure Login using Service Principal
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
          client-secret: ${{ secrets.AZURE_CLIENT_SECRET }}

      # 4️⃣ Verify Azure Connection
      - name: 🔎 Verify Azure Connection
        run: |
          echo "🔍 Checking Azure authentication..."
          az account show --output table
          echo "✅ Azure authentication successful!"

      # 5️⃣ Setup Terraform
      - name: ⚙️ Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.13.3

      # 6️⃣ Terraform Init
      - name: 🧩 Terraform Init
        run: |
          cd $TF_ROOT_DIR
          terraform init -upgrade

      # 7️⃣ Terraform Validate
      - name: ✅ Terraform Validate
        run: |
          cd $TF_ROOT_DIR
          terraform validate

      # 8️⃣ Terraform Plan
      - name: 🧮 Terraform Plan
        id: tfplan
        run: |
          cd $TF_ROOT_DIR
          terraform plan -out=tfplan -input=false
          terraform show -no-color tfplan > tfplan.txt

      # 9️⃣ Upload Terraform Plan
      - name: 📦 Upload Terraform Plan
        uses: actions/upload-artifact@v4
        with:
          name: terraform-plan
          path: ${{ env.TF_ROOT_DIR }}/tfplan.txt

      # 🔟 Display Plan Summary in Actions UI
      - name: 🪶 Add Terraform Plan Summary
        run: |
          echo "### Terraform Plan Summary 📋" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`terraform" >> $GITHUB_STEP_SUMMARY
          head -n 50 $TF_ROOT_DIR/tfplan.txt >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "...(full plan uploaded as artifact)" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "✅ Plan summary added to Actions UI."

      # 11️⃣ Manual Approval before Apply
      - name: ⏳ Manual Approval before Apply
        uses: trstringer/manual-approval@v1
        with:
          approvers: sudhir81
          minimum-approvals: 1
          timeout-minutes: 30
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 12️⃣ Terraform Apply
      - name: 🚀 Terraform Apply
        run: |
          cd $TF_ROOT_DIR
          terraform apply -auto-approve tfplan

      # 13️⃣ Upload Terraform State
      - name: 📤 Upload AKS Outputs
        uses: actions/upload-artifact@v4
        with:
          name: aks-outputs
          path: ${{ env.TF_ROOT_DIR }}/terraform.tfstate

      # ✅ 14️⃣ Final Success Summary
      - name: 🎉 Deployment Summary
        if: success()
        run: |
          echo "-----------------------------------"
          echo "✅ AKS Deployment Completed Successfully!"
          echo "🌍 Environment: ${{ github.event.inputs.environment }}"
          echo "-----------------------------------"
