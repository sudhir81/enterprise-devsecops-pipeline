name: 🚀 Enterprise AKS + DevOps Platform CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  security-events: write

env:
  TF_VERSION: 1.13.3
  TF_ROOT_DIR: project4-enterprise-aks-devops-platform
  IMAGE_NAME: sampleapp
  ACR_NAME: devopsacr001
  AKS_CLUSTER_NAME: devops-aks
  AKS_RESOURCE_GROUP: rg-devsecops-dev

jobs:
  terraform-validate:
    name: 🧩 Terraform Validate + Security Scan
    runs-on: ubuntu-latest

    steps:
      # ✅ Checkout repo
      - name: Checkout repository
        uses: actions/checkout@v4

      # ✅ Setup Terraform
      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TF_VERSION }}

      # ✅ Diagnostic
      - name: Diagnostic – Show folder structure
        run: |
          echo "=== Current Working Directory ==="
          pwd
          echo "=== Folder Tree ==="
          ls -R ./${{ env.TF_ROOT_DIR }}
        working-directory: ${{ github.workspace }}

      # ✅ Validate Terraform path exists
      - name: Validate Terraform Path
        run: |
          TARGET_PATH="${{ github.workspace }}/${{ env.TF_ROOT_DIR }}/infrastructure/aks"
          echo "🔍 Checking for path: $TARGET_PATH"
          if [ ! -d "$TARGET_PATH" ]; then
            echo "❌ ERROR: Terraform directory not found at $TARGET_PATH"
            echo "Available folders:"
            ls -R ${{ github.workspace }}/${{ env.TF_ROOT_DIR }}
            exit 1
          fi
          echo "✅ Terraform path exists: $TARGET_PATH"

      # ✅ Terraform Init + Validate
      - name: Terraform Init (No Backend)
        run: terraform init -backend=false
        working-directory: ${{ env.TF_ROOT_DIR }}/infrastructure/aks

      - name: Terraform Validate
        run: terraform validate
        working-directory: ${{ env.TF_ROOT_DIR }}/infrastructure/aks

      # ✅ Checkov Scan (SARIF)
      - name: Checkov Scan (SARIF)
        uses: bridgecrewio/checkov-action@v12
        with:
          directory: ${{ env.TF_ROOT_DIR }}/infrastructure/aks
          framework: terraform
          soft_fail: true
          output_format: sarif
          output_file_path: results.sarif

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: results.sarif

      # ✅ Generate Checkov JSON Report
      - name: Generate Checkov JSON Report
        run: |
          echo "🧮 Generating Checkov JSON report..."
          docker run --rm \
            -v $(pwd):/tf ghcr.io/bridgecrewio/checkov:latest \
            -d /tf/${{ env.TF_ROOT_DIR }}/infrastructure/aks \
            --framework terraform \
            --output json \
            --output-file-path /tf/checkov_report.json
        working-directory: ${{ github.workspace }}

      # ✅ Build Enhanced HTML (Summary + Pie Chart)
      - name: Build Enhanced HTML Report
        run: |
          echo "🎨 Building enhanced HTML dashboard..."
          TOTAL=$(jq '.summary.total' checkov_report.json)
          PASSED=$(jq '.summary.passed' checkov_report.json)
          FAILED=$(jq '.summary.failed' checkov_report.json)
          SKIPPED=$(jq '.summary.skipped' checkov_report.json)

          echo "<html><head><title>Checkov Terraform Compliance Report</title>
          <style>
            body { font-family: Arial; background:#f8f9fa; margin:30px; }
            h1 { color:#222; }
            table { border-collapse: collapse; width: 60%; margin-bottom:20px; }
            th, td { border: 1px solid #ccc; text-align: left; padding: 8px; }
            th { background-color: #343a40; color: white; }
            .passed { color: green; font-weight: bold; }
            .failed { color: red; font-weight: bold; }
            .skipped { color: orange; font-weight: bold; }
            .pie { width:200px; height:200px; border-radius:50%; margin:20px 0;
                   background:conic-gradient(green 0% calc(${PASSED}/$TOTAL*100%), 
                                             red calc(${PASSED}/$TOTAL*100%) calc((${PASSED}+${FAILED})/$TOTAL*100%), 
                                             orange calc((${PASSED}+${FAILED})/$TOTAL*100%) 100%); }
            pre { background:#fff; border:1px solid #ccc; padding:10px; border-radius:6px; overflow:auto; }
          </style>
          </head><body>" > checkov_report.html

          echo "<h1>✅ Terraform Compliance Summary</h1>
          <div class='pie'></div>
          <table>
            <tr><th>Status</th><th>Count</th></tr>
            <tr><td class='passed'>✅ Passed</td><td>$PASSED</td></tr>
            <tr><td class='failed'>❌ Failed</td><td>$FAILED</td></tr>
            <tr><td class='skipped'>⚠️ Skipped</td><td>$SKIPPED</td></tr>
            <tr><td><b>Total</b></td><td><b>$TOTAL</b></td></tr>
          </table>
          <h2>Full JSON Report</h2><pre>" >> checkov_report.html

          cat checkov_report.json | jq '.' >> checkov_report.html
          echo "</pre></body></html>" >> checkov_report.html
        working-directory: ${{ github.workspace }}

      - name: Upload Checkov HTML Report
        uses: actions/upload-artifact@v4
        with:
          name: checkov-html-report
          path: ${{ github.workspace }}/checkov_report.html

      # ✅ Install OPA
      - name: Install OPA
        run: |
          curl -L -o opa https://openpolicyagent.org/downloads/latest/opa_linux_amd64
          chmod +x opa
          sudo mv opa /usr/local/bin/

      # ✅ Run OPA Policy Evaluation
      - name: Run OPA Policies
        run: |
          terraform show -json > plan.json || true
          echo "⚙️ Evaluating OPA policies..."
          opa eval --data ${{ env.TF_ROOT_DIR }}/policies/opa --input plan.json "data.terraform.policy.deny" \
            --format=json > opa_result.json || true

          echo "<html><head><title>OPA Policy Report</title></head><body><h2>OPA Policy Violations</h2><pre>" > opa_report.html
          cat opa_result.json | jq '.' >> opa_report.html
          echo "</pre></body></html>" >> opa_report.html

      # ✅ Combine Reports into Unified Dashboard
      - name: Combine Reports into Unified Dashboard
        run: |
          echo "🧠 Combining Checkov + OPA reports..."
          echo "<html><head><title>Unified Compliance Dashboard</title></head><body>" > compliance_dashboard.html
          echo "<h1>🌐 Enterprise DevSecOps Compliance Dashboard</h1>" >> compliance_dashboard.html
          echo "<h2>✅ Checkov Infrastructure Scan</h2>" >> compliance_dashboard.html
          cat checkov_report.html >> compliance_dashboard.html
          echo "<hr><h2>🧠 OPA Policy Validation</h2>" >> compliance_dashboard.html
          cat opa_report.html >> compliance_dashboard.html
          echo "</body></html>" >> compliance_dashboard.html

      - name: Upload Unified Compliance Dashboard
        uses: actions/upload-artifact@v4
        with:
          name: compliance-dashboard
          path: ${{ github.workspace }}/compliance_dashboard.html

      - name: Print Summary
        run: |
          echo "✅ Terraform + Security checks completed successfully"
          echo "📊 Dashboard available under Artifacts → compliance-dashboard.html"
          echo "📄 SARIF report uploaded to Security → Code scanning alerts"

